function somma(a, b) {
  return a + b;
}

const Sommaarrow = (a, b) => a + b;

function ConcatenaStinga(str1, str2) {
  return `${str1} ${str2}`;
}

const ConcatenaStingaarrow = (str1, str2) => `${str1} ${str2}`;

// Anonymus function
const Moltiplica = function (a, b) {
  return a * b;
};

// Anonymus Function Arrow
const MoltiplicaArroww = (a, b) => a * b,
  Getrettangolo = (largezza, altezza) => largezza * altezza;

function Hello(name = "Mario") {
  return `Ciao ${name}`;
}

function StmpaIndirizzo(via, città) {
  return `abito in via ${via} della città di ${città}`;
}

const Benvenuto = (nome) => `Ciao ${nome}`;
